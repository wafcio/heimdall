div#map style="width: auto; height: 400px;"
- points.each do |point|
  div.marker data-id="#{point.id}" data-name="#{point.name}" data-lat="#{point.latitude}" data-lng="#{point.longitude}" data-marker="#{point.category ? point.category.image_url : "default"}"
script src="https://maps.googleapis.com/maps/api/js?key=#{ENV["GOOGLE_MAPS_API"]}&callback=initMap"
